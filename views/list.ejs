<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title></title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="public/css/styles.css">
    </head>
    <body>
        <div class="box" id="heading">
            <h1><%= kindOfDate %></h1>
        </div>
        <div class="box">
            
                <% for (var i = 0; i < newListItem.length; i++) { %>
                    <form action="/delete" method="post">
                        <div class="item">
                            <input type="checkbox" onchange="this.form.submit()" name ="checkbox" value=<%=newListItem[i]._id%>>
                            <p><%= newListItem[i].name %></p>
                        </div>
                        <input type="hidden" name="listName" value=<%=kindOfDate%>></input>
                    </form>
                <% } %>
            
                <form action="/" method="post" class="item">
                    <input type="text" name="newItem" placeholder="New Item">
                    <button type="submit" name="list" value=<%=kindOfDate%> >+</button>
                </form>
        </div>
        
        <script src="" async defer></script>
    </body>
</html>
